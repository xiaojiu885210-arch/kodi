# 安装

下载 **Releases** 里的 `plugin.video.douyin-1.2.0.zip`，不要点 GitHub 绿色「代码」按钮里的「下载 ZIP 文件」（那个是 `kodi-main.zip`，Kodi 会报结构无效）。

1. 把 zip 原封不动拷到电视 / 盒子（U 盘最稳，尽量别走 smb）。
2. Kodi → 设置 → 系统 → 插件 → 打开「未知来源」。
3. 插件 → 从 zip 文件安装 → 选 `plugin.video.douyin-1.2.0.zip`。
4. 视频插件 → 抖音。

## Cookie 登录

1. 电脑打开 https://www.douyin.com 并登录（扫码、扫脸都在网页完成）。
2. 按 F12 → 应用程序 → Cookies → `https://www.douyin.com`。
3. 复制 `sessionid` 的值。
4. 插件首页点「登录抖音账号」粘贴。也可以存成 U 盘上的 `douyin_cookie.txt` 再选「从文本文件读取」。

登录一次会保存在本机。之后可看「我的关注」「我喜欢」。
